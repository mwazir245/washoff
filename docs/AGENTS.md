\# WashOff Agent Instructions



\- This is NOT a marketplace.

\- Hotels must never manually choose providers.

\- The platform is Arabic-first.

\- Preserve RTL support.

\- Preserve the current domain/application/infrastructure boundaries.

\- Prefer incremental changes over large rewrites.

\- Keep matching logic explainable and auditable.

\- Do not bypass the application service layer.

